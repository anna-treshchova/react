export class AppError extends Error {
    constructor(errConfig, status, details) {
        super(errConfig?.message || 'Unknown error');

        this.code = errConfig?.code || 'UNKNOWN_ERROR';
        this.status = status;
        this.details = details;

        this.isOperational = true;

        Error.captureStackTrace(this, this.constructor);
    }
}

/*──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────


 new User() — це створенням екземпляра класу

 Це єдина синтаксична конструкція (new не викликається без User() й навпаки).
 Вона запускає процес, який складається з двох етапів: Allocation (виділення) та Initialization (ініціалізація).

  1. Етап Аллокації (Memory Allocation & Prototype Link)

       Коли рушій бачить оператор new, він викликає внутрішній свій внутрішній алгоритм [[Construct]]
       й передає туди наш класс Class як інструкцію

       Цей алгоритм виконує наступне:

          1.1 Виділяє блок пам'яті під новий об'єкт

          1.2 Автоматично записує у внутрішню властивість [[Prototype]] (те, що в коді доступно як __proto__)
              посилання на User.prototype

              User.prototype - в об'єкті prototype, який є властивостю User зберігаються методи класу, які мають
              бути спільними для всіх його екземплярів (об'єктів)


                                     ──────────────────────────── Class is syntactic sugar ─────────────────────────────

Класи в JS це синтаксичний цукор, тобто це просто візуальне поліпшення для розробників, яке про яке движок не знає й
 не розуміє.

  Класи реалізовані через фцнкції та маніпуціїї наслідування, класи це лише , яке движок не розуміє, воно трансформується в звичайні функції та прототипування й з цим вже працює рушій?
  Рушій (наприклад, V8) взагалі не має всередині себе окремої сутності Клас.


  На етапі парсингу та компіляції він миттєво трансформує клас у звичайну функцію-конструктор та маніпуляції з її prototype

          1.3 На цьому етапі об'єкт є порожнім відносно власних властивостей (own properties).
              Але він вже не просто абстрактний шматок пам'яті — він уже має зв'язок із прототипом свого класу.



─────────────────────────────────────────   V8 Engine (рушій Chrome/ Node.js) ──────────────────────────────────────────

 Завантаження та розміщення:
 ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

      Рушій V8 — це невіддільна частина Chrome або Node.js.

      Коли ми завантажуємо та встановлюємо щось з них на комп'ютер, ми фізично записуємо на наш диск машинний код
      усього рушія V8, включаючи модулі Parser, Ignition, TurboFun.


  Запуск в пам'яті:
 ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
      Коли ми відкриваємо Chrome або запускаємо Node.js, машинний код V8 Engine завантажується в оперативну пам'ять
      і починає виконуватися нашим фізичним процесором


 Обробка bytecode (без TurboFan):
 ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
      Наш JavaScript-код, який парсер перетворив на AST й потім базовий компілятор (Ignition) перетворив на bytecode,
      лежить у пам'яті як набір даних.

      Якщо цей код не визнано «гарячим», він ніколи не потрапляє в TurboFan і не перекладається в машинний код.

 Роль процесора:
‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
 Процесор у цьому сценарії не знає і не бачить наш байт-код. Процесор бачить і виконує лише рідний машинний код модуля
 Ignition.

Процесор комп'ютера завжди виконує виключно машинний код. Якщо ділянка JavaScript-коду не пройшла через оптимізатор
TurboFan, вона залишається у вигляді байт-коду.

У цьому випадку фізичний процесор виконує машинний код самого модуля Ignition (який уже встановлений на комп'ютері у
складі Chrome/Node.js).
 Процесор крутить інструкції Ignition, а вже Ignition здійснює операціїї прописані в бфйт-коді


────────────────────────────────────  Архітектура V8 Engine (Рушія Chrome/ Node.js) ────────────────────────────────────

 Рушій V8 — це складна програмна система, яка складається з трьох ключових архітектурних модулів (компонентів),
 що працюють як єдиний конвеєр:


 1. Парсер (Parser)
‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

    ПРИЗНАЧЕННЯ: Модуль синтаксичного аналізу

    ФУНКЦІЯ: Приймає сирний текст вихідного коду (JavaScript), аналізує його символ за символом і синхронно будує
             AST (Abstract Syntax Tree), паралельно валідуючи код на наявність синтаксичних помилок (SyntaxError)



 2. Ignition (Інтерпретатор або Базовий компілятор)
‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

    ПРИЗНАЧЕННЯ: Модуль швидкого старту та виконання проміжного коду

    ФУНКЦІЯ: Приймає згенероване дерево AST, транслює його у низькорівневий універсальний байт-код (bytecode) і відразу
             починає його виконання (інтерпретацію), працюючи як віртуальна машина


                                      ─────────────────── Profiler (Внутрішній Механізм Моніторингу) ───────────────────

                                        Профілювання (Profiler) — це внутрішній механізм моніторингу, який є частиною
                                        машинного коду самого інтерпретатора Ignition


                                        СУТЬ РОБОТИ МЕХАНІЗМУ:

                                           1. Створення лічильника:
                                           ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

                                              Коли Ignition починає виконувати bytecode файлу рядок за рядком й доходить
                                              до оголошення функції, він дає команду процесору виділити комірку пам'яті
                                              під новий об'єкт функції.


                                      ──────────────── RAM (Random Access Memory) and who controls it  ─────────────────


                                        OS (Operation System):
                                        ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
                                           Одноосібним і головним розпорядником усієї пам'яті на комп'ютері є виключно
                                           Операційна Система (macOS, Windows, Linux).

                                           Коли ми запускаємо Chrome або Node.js, OS виділяє йому початковий, великий
                                           шматок сирої оперативної пам'яті.

                                           Якщо програмі (в нашому випадку рушію V8) у якийсь момент стає мало місця,
                                           він робить офіційний системний запит до ОС на оотримання додаткового шматку
                                           пам'яті


                                              V8 Engine:
                                        ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
                                           Ось цей виділений операційною системою шматок пам'яті рушій V8 і називає
                                           своїм Heap (купою).

                                           ОС туди більше не втручається, V8 має свій власний потужний менеджер пам'яті,
                                           який тепер стає повноправним господарем всередині цієї ділянки.


                                           НАРІЗКА ПРОСТОРУ:

                                              Коли Ignition доходить до оголошення нової функції, менеджер пам'яті
                                              (Heap Allocator) самостійно вираховує потрібну кількість байтів під JS
                                              об'єкт-функції і знаходить під них вільні адреси всередині Heap.

                                           ВНУТРІШНЯ РОЗМІТКА:

                                              Цей же Heap Allocator нарізає виділені байти за строгою структурою класу
                                              JSFunction: виділяє перші байти під заголовок, наступні — під посилання
                                              на байт-код, і окрему комірку — під посилання на вектор зворотного зв'язку,
                                              де сидить лічильник.

                                           ФІЗИЧНИЙ ЗАПИС:

                                              Ignition генерує машинну інструкцію для процесора з уже готовою фізичною
                                              адресою цієї комірки.

                                              Процесор просто виконує команду і записує туди початковий нуль, занулюючи
                                              лічильник.

V8 шукає вільне місце всередині своєї ділянки: Його внутрішній менеджер дивиться на свій Heap і бачить, що за адресою 0x20A0 є вільні 40 байтів.

V8 робить розмітку: Рушій сам, своїм кодом, вирішує, що за цією адресою тепер лежить функція. Він каже: за адресою 0x20A0 буде заголовок, а за адресою 0x20A4 — комірка лічильника.

Процесор — сліпо виконує інструкції V8 щодо запису та читання бітів у межах адрес, які дозволила використовувати операційна система.

                                      ──────────────────────────────────────────────────────────────────────────────────


                                                Коли Ignition створює об'єкт функції в пам'яті (при її оголошенні ), рушій виділяє всередині цього об'єкта комірку
    пам'яті під лічильник викликів та лічильник зворотних переходів у циклах

 Коли процесор виконує машинний код Ignition і запускає нашу функцію, перше, що він робить за замовчуванням:

     1. Збільшує лічильник цієї функції на +1

     2. Перевіряє: "Чи перевищило це число певний ліміт (поріг гарячості)?"

     Ось цей процес збільшення чисел та їхньої перевірки і є механізмом профілювання (Profiler).



   Як він пов'язаний з базовим компілятором та TurboFan?
      Він слугує зв'язковим містком (тригером) між Ignition та TurboFan:

     Поки код «холодний»: Лічильник росте повільно (наприклад, функція викликалася 5 разів).
     Процесор просто крутить код Ignition.

     Код стає «гарячим»: Наприклад, функція викликалася 10 000 разів (або всередині неї прокрутився важкий цикл).
    Лічильник пробиває встановлений розробниками V8 поріг.

      Спрацьовує тригер: Механізм профілювання каже рушію: "Стоп, ця функція аномально гаряча!".
      Він ставить цю функцію в чергу на JWT компіляцію для TurboFan

      І поки TurboFan у фоновому потоці оптимізує цю функцію, процесор продовжує крутити її через Ignition.
      Як тільки TurboFan закінчує — посилання міняється





 3. TurboFan (Оптимізуючий JIT-компілятор)
‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

   JIT (Just In Time) компілятор — це компілятор, який виконує оптимізуючу компіляція прямо під час виконання коду
   Ignition модулем

    ПРИЗНАЧЕННЯ: Модуль максимальної оптимізації продуктивності

    ФУНКЦІЯ: Працює паралельно з виконанням байт-коду.

             Коли така функція знайдена, TurboFan забирає її, оптимізує та компілює з байт-коду безпосередньо в чистий
             машинний код під конкретну архітектуру процесора (Intel/ARM), повністю усуваючи рушій з ланцюжка виконання
             цього шматка коду


             Кожна JS-функція в пам'яті має посилання на свій код виконання (JSFunctionData).

До оптимізації: це посилання веде на модуль Ignition, який розшифровує байт-код.

Після оптимізації: TurboFan перезаписує це посилання, вказуючи на адресу згенерованого машинного коду.

При наступному виклику функції Ignition просто перенаправляє процесор за новим посиланням. Процесор починає виконувати машинний код функції напрямую на рівні заліза, минаючи логіку інтерпретатора, а після завершення роботи функції — повертає керування назад в Ignition.


─────────────────────────────────────── Схематичний ланцюжок обробки коду в V8: ────────────────────────────────────────

        JS-Текст ──> [ Parser ] ──> AST ──> [ Ignition ] ──> Bytecode ──> [ TurboFan ] ──> Machine Code (Процесор)

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────




──────────────────────────────────────────────────  Парсинг (Parsing)  ─────────────────────────────────────────────────

 Парсинг ─ рушій читає код символ за символом, перетворюючи його на спеціальну деревоподібну структуру — AST
 (Abstract Syntax Tree)


 ВАЖЛИВО:

     ◦ Саме під час цього процесу (на етапі побудови вузлів дерева) рушій синхронно перевіряє код на синтаксичні помилки

     ◦ Якщо код порушує правила мови, дерево ламається і рушій викидає SyntaxError

     ◦ Тобто перевірка синтаксису та побудова AST — це синхронний процес
       ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

          Рушій перевіряє код на помилки прямо в момент спроби побудувати черговий вузол дерева

          Помилка — це просто технічна неможливість побудувати правильне дерево з того тексту, який написав розробник



 НАЩО ПОТРІБЕН ПАРСИНГ?

 Рушій не може виконувати код просто як текст й ось чому:

       let результат = 2 + 3 * 4;

 Якщо читати це просто як текст, то рушій мав би зробити: 2 + 3 = 5, а потім 5 * 4 = 20
 Але це математично неправильно, бо множення має вищий пріоритет

 Тож для виразу 2 + 3 * 4 рушій побудує таке дерево:

             +
            / \
           2   *
              / \
             3   4

 Дивлячись на це дерево, компілятор чітко бачить: операція * знаходиться глибше, тому її треба виконати першою,
 а її результат передати наверх до операції +



 З ЦЬОГО МИ РОЗУМІЄМО ЩО:

 Текст коду — це просто плоский рядок символів. У ньому немає глибини.

 Комп'ютер не може просто читати текст зліва направо і магічно знати, що функція лише оголошена (declared),
 її код треба запам'ятати (hoisting) й поки нічого не робити, а виконувати цей код лише при її виклику

     hoisting — підняття оголошень функцій у верх області видимості перед безпосереднім виконанням коду
     ‾‾‾‾‾‾‾‾

 AST (Abstract Syntax Tree) якраз і перетворює цей плоский текст у ієрархічну структуру.

 Завдяки цьому дереву рушій чітко бачить:

     Порядок і пріоритети:
     ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
         Що лежить на найглибших гілках дерева, те виконується першим (як множення у прикладі, або внутрішні дужки в
         математиці)


     Вкладеність і залежності:
     ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
         Області видимості змінних (scope). Дерево чітко показує ієрархію коду: який шматок знаходиться всередині якого
         (які змінні належать конкретній функції, а які є глобальними)



 Тобто, коли рушій має перед очима AST, для нього порядок виконання стає очевидним: він просто йде по гілках дерева
 знизу вгору: обчислив найглибшу гілку ——> підняв результат вище ——> виконав наступну операцію




──────────────────────────────────────────────  Компіляція (Compilation)  ──────────────────────────────────────────────

 Компіляція ─ це процес перетворення дерева AST у низькорівневий код (байт-код або машинний код), який спроможний
 зрозуміти та виконати процесор комп'ютера.

 Взагалі загальний термін компіляція ─ це процес перекладу коду з однієї мови на іншу (як правило, з вищого рівня на
 нижчий)

 ВАЖЛИВО:

 ◦ Комп'ютерний процесор фізично не вміє читати текст програми і навіть структуру дерева AST.
   Він розуміє лише найпростіші бінарні інструкції (нулі та одиниці).

 ◦ У сучасних JS-рушіях (як V8) компіляція не відбувається "один раз перед запуском" (як у C++ чи Rust)
   Вона відбувається безпосередньо під час роботи програми. Це називається JIT-компіляція (Just-In-Time)
                                                                                          ‾‾‾‾‾‾‾‾‾‾‾‾‾‾


ЯК САМЕ ВІДБУВАЄТЬСЯ КОМПІЛЯЦІЯ В JS? (ДВОРІВНЕВИЙ КОНВЕЄР ВАШОГО РУШІЯ)

Оскільки JavaScript — динамічна мова, рушій змушений балансувати між швидким стартом програми та швидкістю її роботи.
Для цього процес компіляції розділений на два окремі компілятори всередині рушія:

БАЗОВА КОМПІЛЯЦІЯ (Інтерпретатор — Ignition у V8)
────────────────────────────────────────────────────────────────────────
Рушій бере дерево AST і максимально швидко перекладає його у байт-код (bytecode) — проміжну, компактну мову.

Як це працює: Байт-код створюється миттєво, і рушій одразу починає його виконувати.

Мета: Запустити вашу сторінку або скрипт без жодних затримок. На цьому етапі код працює стабільно, але не на максимальній швидкості.

ОПТИМІЗУЮЧА КОМПІЛЯЦІЯ (JIT-компілятор — TurboFan у V8)
────────────────────────────────────────────────────────────────────────
Поки базовий байт-код виконується, спеціальний менеджер (Profiler) стежить за роботою програми. Він шукає «гарячі ділянки коду» — функції, які викликаються найчастіше (наприклад, цикли або математичні прорахунки у грі чи travel-додатку).

Як тільки функція стає «гарячою», рушій відправляє її в оптимізуючий компілятор, який переводить її з байт-коду
прямо в чистий машинний код (машинні інструкції процесора).

Результат: Ця конкретна функція починає літати на максимальній швидкості заліза.

З ЦЬОГО МИ РОЗУМІЄМО ЩО:

JavaScript не є просто "інтерпретованою" мовою, де код читається рядок за рядком під час виконання.
Він повністю компілюється, але робить це «на льоту» (Just-In-Time).

Процес перетворення коду виглядає як ланцюжок:

 [ Текст коду ] ──(Парсинг)──> [ Дерево AST ] ──(Ignition)──> [ Байт-код ] ──(TurboFan)──> [ Машинний код ]
ТУТ Є ОДИН НЮАНС ДИНАМІЧНОЇ МОВИ: ДЕОПТИМІЗАЦІЯ (Deoptimization)
‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
Оскільки в JS типи змінних можуть змінюватися, компілятор TurboFan робить "припущення", коли створює машинний код.
Наприклад, якщо функція sum(a, b) 1000 разів приймала числа, компілятор створює надшвидкий машинний код суто для чисел.

Якщо на 1001-й раз ви передасте туди рядки: sum("1", "2"), машинний код спотикається.
Відбувається деоптимізація: рушій викидає оптимізований машинний код і повертається назад до виконання базового байт-коду.

Тобто, коли рушій має перед очима AST, він використовує його як фінальне креслення для генерації інструкцій процесору:
спочатку швидко робить загальний байт-код, а потім вибірково перетворює найважливіші шматки в ультимативно швидкі нулі та одиниці.










Байт-код (Bytecode) та Машинний код (Machine code). Це не одне й те саме.

Різниця між ними фундаментальна: для якого саме «процесора» ці байти написані.


1. Машинний код (Physical Machine Code)
 ◦ Це байти, які розуміє твій фізичний процесор (Intel, AMD або Apple Silicon M1/M2/M3), що стоїть у твоєму комп'ютері.

 ◦ Хто його виконує: Залізо (кремнієвий чіп).

 ◦ Особливість: Він прив'язаний до архітектури. Машинний код для процесора Intel (x86) — це зовсім інші байти, ніж машинний код для процесора Apple Silicon (ARM). Вони не зрозуміють код один одного.



2. Байт-код (Virtual Machine Code)
 ◦ Ці байти написані для віртуального процесора (всередині самого рушія V8), якого фізично не існує в природі. Це абстрактна, вигадана інструкція.

 ◦ Хто його виконує: Програма-інтерпретатор (Ignition у V8). Вона читає ці «абстрактні байти» і сама виконує відповідні дії в системі.

 ◦  Особливість: Він універсальний. Рушію V8 байдуже, на якому комп'ютері він запущений — на Mac, Windows чи Linux. Він скрізь генерує один і той самий універсальний байт-код із твого JS-тексту.






І машинний код, і байт-код — це дійсно просто потоки байтів (чисел від 0 до 255, які в бінарному вигляді є нулями та одиницями). Але вони мають абсолютно різну комбінацію, різний набір інструкцій та різний «словник».

Давай подивимось, чому ці комбінації відрізняються:

1. Комбінація байт-коду (Словник рушія V8)
Ці байти складені за правилами, які придумали розробники Google V8.
Наприклад, рушій V8 має внутрішню інструкцію для додавання двох значень, яка називається Ldar або Add. У потоці байт-коду це може бути зашидовано як конкретний байт — наприклад, 0x5B.

Коли рушій бачить цей байт, він знає: "О, розробники V8 навчили мене, що 0x5B означає 'додати два числа'. Зараз я сам виконаю це через свій C++ код".

Фізичний процесор комп'ютера, якщо підсунути йому цей байт 0x5B, або зробить зовсім іншу дію, або взагалі видасть помилку, бо в його власному словнику цей байт означає щось інше або не існує.

2. Комбінація машинного коду (Словник заліза Intel / Apple Silicon)
Ці байти складені за правилами інженерів, які створювали фізичний мікропроцесор.
Наприклад, для процесорів Intel (архітектура x86) інструкція додавання (ADD) кодується зовсім іншими байтами (наприклад, 0x01 або 0x05).

Коли цей байт потрапляє на кремнієвий чіп, всередині процесора фізично замикаються потрібні транзистори (блок ALU), і залізо самостійно, на рівні електричних сигналів, миттєво складає ці числа.

Резюме для конспекту:
І байт-код, і машинний код — це байти. Але:

Байт-код — це комбінація байтів, яку розуміє тільки програма (рушій V8). Це "штучна" мова.

Машинний код — це комбінація байтів, яку розуміє конкретне залізо (процесор комп'ютера). Це "рідна" мова мікросхем.







─────────────────────────────────────────  Хронологія життя функції в пам'яті  ─────────────────────────────────────────

 Уявимо, що в нащому файлі написаний такий код:

     │ const user = "Hanna";
     │
     │ function greet(name) {
     │   return `Hello, ${name}!`;
     │ }                                 <── function declaration
     │
     │ greet(user);                      <── function call



 1. Парсинг (Parser)
    Рушій читає цей код символ за символомй на його основі будує дерево AST.

 2. Генерація байт-коду (Ignition як компілятор)
   Ignition бере AST і перекладає весь його у байт-код.

     Але сама функція як об'єкт JavaScript ще не створена! Є просто заготовка її майбутнього коду.

 3. Виконання головного потоку коду — НАРОДЖЕННЯ ФУНКЦІЇ
        Ignition починає виконувати байт-код файлу рядок за рядком.

        Він виконує перший рядок: створює в пам'яті змінну user зі значенням "Hanna".

        Далі доходить до рядків оголошення функції greet.
        САМЕ В ЦЕЙ МОМЕНТ Ignition дає команду процесору виділити пам'ять у Heap під новий об'єкт функції.

       Що з'являється в пам'яті прямо зараз:
           В оперативній пам'яті народжується повноцінний JS-об'єкт функції.
           У нього записуються стандартні властивості (name: "greet", length: 1), а також те саме приховане поле JSFunctionData.

           І Ignition одразу записує в це поле посилання на самого себе (на інтерпретатор) та на той масив байт-коду
           (BytecodeArray), який він підготував на Кроці 2.

           Тепер функція офіційно живе в пам'яті. Вона повністю готова, але вона ще жодного разу не викликалася.

 44. Перший виклик функції (Виконання через Ignition)
                          Код іде далі й доходить до рядка greet(user).

                  Рушій дивиться на об'єкт greet у пам'яті, бере посилання з JSFunctionData, бачить, що воно веде на Ignition, і процесор починає крутити байт-код цієї функції через інтерпретатор.

           Крок 5. Оптимізація (TurboFan)
                 Якщо цей виклик повториться тисячі разів, підключиться TurboFan. Він згенерує машинний код, покладе
                 його в пам'ять і перезапише посилання в тому самому полі JSFunctionData, яке було створено ще на Кроці 3.










 Комп'ютерний процесор не вміє читати навіть AST-дерево. Йому потрібні конкретні низькорівневі інструкції
 (машинний код: нулі та одиниці).

Компіляція — це процес перетворення цього AST-дерева на ефективний машинний код.

В сучасних рушіях JS (як V8) цей процес дворівневий:

Базова компіляція: Спеціальний компілятор (Ignition) швидко перетворює дерево на байт-код (проміжну мову),
щоб програма могла запуститися миттєво.

Оптимізуюча компіляція (JIT): Коли код уже працює, інший компілятор (TurboFan) стежить за функціями, які викликаються
 найчастіше, і на льоту перекомпілює їх прямо в машинний код (binary/machine code) для максимальної швидкості.

 */