import { transformToErrorResponse } from './error-response';

export const fetchCriticalData = async (dispatch, thunkActionCreator, args) => {
    const promise = dispatch(thunkActionCreator(args));

    try {
        await promise.unwrap();
    } catch (err) {
        throw transformToErrorResponse(err);
    } finally {
        promise.unsubscribe();
    }
}


/*

════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
                                     Що саме можна передавати в store.dispatch?
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

 Оригінальний dispatch з бібліотеки Redux вміє приймати ТІЛЬКИ ПРОСТІ ОБ'ЄКТИ (actions) типу { type: 'auth/sendCode' }.
 Якщо ми передамо в нього функцію — він миттєво викличе помилку.

 Те, що ми можемо писати dispatch(thunk) — це повністю заслуга мідлвари redux-thunk.

 Вона вбудована в пакет @reduxjs/toolkit і автоматично активується при виклику функції configureStore (функція
 ініціалізації стору)






⚙️ ПІДКАПОТНА КОНФІГУРАЦІЯ: Що насправді відбувається при виклику `configureStore`
══════════════════════════════════════════════════════════════════════════════════

Створення стору та налаштування мідлвар — це один суцільний, нерозривний процес.
Коли відпрацьовує configureStore, двигун Redux Toolkit виконує наступні кроки
суворо один раз при старті додатка:

1. Ініціалізація ядра стору:
   Спочатку створюється базовий об'єкт стору, який отримує свої оригінальні методи
   (`dispatch`, `getState`, `subscribe`). На цьому етапі оригінальний диспатч є "суворим".

   ┌───────────────────────────────────────────────────────────────────────────────┐
   │   const store = configureStore({ reducer: rootReducer });                     │
   │   const originalDispatch = store.dispatch;  ──> вміє приймати ТІЛЬКИ об'єкти  │
   └───────────────────────────────────────────────────────────────────────────────┘


2. Збирання конвеєра мідлвар (Створення замикань):
   RTK збирає масив мідлвар (куди за замовчуванням першою входить `redux-thunk`).
   Кожна мідлвара має трирівневу структуру: `store => next => action => { ... }`.

   Щоб зв'язати їх у єдиний ланцюжок, RTK примусово викликає перші два рівні кожної
   мідлвари по черзі.

     ◦ Рівень 1 (`store`): Мідлвара викликається і отримує живе посилання на об'єкт
       стору. Оскільки доступ до даних надалі йде через динамічний виклик методу
       `store.getState()`, мідлвара завжди бачитиме актуальний стан, а не "знімок".

     ◦ Рівень 2 (`next`): Мідлвара отримує посилання на наступний крок конвеєра.

   В результаті цього виклику кожна мідлвара створює **Closure Scope** (замикання)
   в Купі (Heap), де консервує посилання на `store` та `next`, і повертає кінцеву
   функцію третього рівня `(action) => { ... }`.

   Ця фінальна збірка повністю перезаписує стандартний диспатч додатка:
   ┌───────────────────────────────────────────────────────────────────────────────┐
   │  store.dispatch = (action) => thunkMiddleware(nextMiddleware(originalDispatch))│
   └───────────────────────────────────────────────────────────────────────────────┘
   Усі перші та другі рівні функцій повністю зникають із Call Stack. Обгортки застигають.


3. Тестовий прогін системи (Перший системний екшен):
   Тільки після того, як ланцюжок повністю зібраний і замикання сформовані, Redux
   автоматично диспачить свій перший внутрішній системний екшен: `{ type: '@@redux/INIT...' }`.

   Цей екшен є першим, хто фізично пролітає крізь уже створені функції третього рівня.
   Він проходить крізь увесь конвеєр, долітає до редьюсерів і змушує їх повернути
   свій `initialState`, формуючи початкове дерево стану системи.






 Коли викликається configureStore з Redux Toolkit, під капотом відбувається:

 1. Ініціалізується стор:

       1.1 отримує свої оригінальні методи dispatch, getState ...

                   ┌───────────────────────────────────────────────────────────────────────────────┐
                   │   const store = configureStore(rootReducer);                                  │
                   │   const originalDispatch = store.dispatch;   ──> вміє приймати лише об'єкти   │
                   └───────────────────────────────────────────────────────────────────────────────┘


       1.2 Створюється ланцюжок мідлвар який підключається безповередньо до стору (з дефолтний мідлвар та з тих які ми
        самі можемо передати в об'єкт конфігурації configureStore

       1.3 Кожна мідлвара має трирівневу структуру виклику: store => next => action => { ... }

              При конфігурації стору Redux диспачить внутрішній системний екшен ({ type: '@@redux/INIT' }), який
              запускає редьюсери, змушуючи їх формувати початковий state, пролітає крізь цей свіжостворений ланцюжок
              мідлвар, змушуючи їх робити замикання й повретати останню функцію яка приймає action,




2. Чи проганяються мідлвари один раз при конфігурації?
Це найцікавіший момент. .


3. Чи буде очікуваний екшен першим ініціалізуючим екшеном?
Так! Перший екшен, який пролетить крізь цей свіжостворений ланцюжок, — це внутрішній системний екшен Redux, який він сам і диспатчить для запуску редьюсерів і формування початкового стейту.
Офіційно в RTK він називається @@redux/INIT.... Тобі не потрібно його ловити, але саме він першим тестує твій ланцюжок мідлвар.





// 2. Створюється ланцюжок мідлвар, де Thunk стоїть першим
const thunkMiddleware = ({ dispatch, getState }) => (next) => (action) => {
  if (typeof action === 'function') {
    return action(dispatch, getState); // 👈 перехоплення
  }
  return next(action);
};

// 3. ⚠️ МАГІЯ: Стору повністю заміняють (перезаписують) метод dispatch!
store.dispatch = (action) => {
  // Наш thunkMiddleware стає "зовнішніми дверима" для будь-якого виклику
  return thunkMiddleware({ dispatch: store.dispatch, getState: store.getState })(originalDispatch)(action);
};
🧬 Що повертає хук useDispatch()?
Коли ти пишеш у компоненті:

JavaScript
import { useDispatch } from 'react-redux';
const dispatch = useDispatch();
Хук useDispatch під капотом робить банальну річ — він просто бере метод dispatch з твого глобального об'єкта store:

JavaScript
// Внутрішня логіка react-redux
function useDispatch() {
  const store = useContext(ReactReduxContext);
  return store.dispatch; // 👈 Повертає ВЖЕ ОБГОРНУТИЙ мідлварою диспатч!
}
🏁 Кінцева картина виконання в коді компонента:
Коли в Main Thread виконується рядок:

JavaScript
dispatch(verifyCode({ code: '1234' }))
Спочатку синхронно обчислюється аргумент — викликається verifyCode(), який миттєво повертає функцію-Танк (Рівень 2).

Замість виразу підставляється Танк. Рядок стає: dispatch(функція_Танк).

Викликається функція dispatch. Але оскільки це вже не оригінальний dispatch, а модифікована мідлварою обгортка, першим ділом починає виконуватися код самої мідлвари redux-thunk.

Мідлвара бачить typeof action === 'function', зупиняє виконання і запускає твій Танк.

Оригінальний dispatch (який зламався б від функції) на цьому етапі взагалі не викликається. Він стоїть у кінці черги й чекає, поки мідлвари не передадуть йому звичайний об'єкт { type: '.../pending' } чи { type: '.../fulfilled' }.

















 яка повністю підміняє (модифікує)
 глобальний dispatch.

 Redux Toolkit мідлвара Thunk підключається автоматично під капотом), бібліотека redux-thunk при ініціалізації робить маніпуляцію: вона бере стандартний dispatch і обгортає його у свою функцію.

Те, що ти викликаєш у компоненті під ім'ям dispatch, — це вже НЕ оригінальний дефолтний диспатч. Це «прокачана» обгортка від мідлвари.

Коли ти пишеш dispatch(thunk), під капотом відбувається наступне:







 store.dispatch вміє приймати два типи даних:

   ◦ actions — звичайні JS б'єкти: { type: '...', payload: ... }  ——> вони йдуть прямо в редюсери

   ◦ thunks — асинхронні функції  ——> вони перехоплюються мідлварою, виконуються асинхронно (чекають на сервер) й
     диспачать чисті об'єкти-екшени



Треба розуміти що ми завжди в діспачі викликаємо actionCreator:

════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
                                       Типи Action Creators у Redux Toolkit (RTK)
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════

 В RTK actionCreators створюються двома способами:

   ◦ Синхронні (автоматично з поля reducers у createSlice) — створюються під капотом RTK для кожної функції-мутації,
     яку ми передаємо в об'єкт reducers.

   ◦ Асинхронні (через createAsyncThunk) — повертають трирівневу структуру з Функцією-Танком (менеджером), яка
     перехоплюється мідлварою для виконання побічних ефектів у WebAPIs.



═══════════════════════════════
 Синхронні Action Creators

 📍 Базові
═══════════════════════════════

                                                    ЯК СТВОРЮЮТЬСЯ:
                                                ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

 ВСЕ ПОЧИНАЄТЬСЯ ЗІ СТВОРЕННЯ СЛАЙСУ:

                                                                  ┌────────────────────────────────────────────────────┐
   1. У функцію createSlice ми передаємо об'єкт конфігурації,     │                                                    │
   де в полі reducers описуємо функції-мутації.                   │     export const authSlice = createSlice({         │
                                                                  │         name: 'auth',                              │
   Функції-мутації ─ це функції, які за допомогою вбудованої      │         initialState,                              │
   бібліотеки Immer під капотом мутують копію стану слайсу.       │         reducers: {                                │
                                                                  │             setError: (state, action) => {         │
                                                                  │                 state.error = action.payload;      │
   2. Коли двигун JavaScript (V8) під час читання єдиного         │             },                                     │
   бандлу доходить до цього рядка й викликає createSlice(),       │         },                                         │
   функція генерує й повертає API-об'єкт, який зберігаєтья        │     })                                             │
   в змінну authSlice.                                            │                                                    │
                                                                  └────────────────────────────────────────────────────┘



                    ──────────────────────────────────────  API-об'єкт authSlice  ──────────────────────────────────────

                      API-об'єкт ─ це об'єкт, який сам по собі не є кінцевим результатом (стейтом), але містить у собі
                      набір інструментів (actions, reducer), за допомогою яких можна реалізувати головну логіку й
                      отримати кінцевий результат

                      createSlice дає нам чітко визначений інтерфейс керування:

                            ◦ actions надають нам інтерфейс для генерації екшенів
                            ◦ reducer ─ інтерфейс для створення глобального стору та керування його станом


                      У цьому API-об'єкті:

                            ◦ Для кожної функції-мутації з об'єкта reducers автоматично створюється Action Creator
                              і додається в поле authSlice.actions

                                 НАПРИКЛАД:

                                 Виклик authSlice.actions.setError('CODE_RESEND_DELAY') поверне звичайний об'єкт екшену:
                                 { type: 'auth/setError', payload: 'CODE_RESEND_DELAY' }


                            ◦ Усі функції-мутації (з поля reducers) та всі обробники екшенів (з поля extraReducers)
                              згортаються під капотом в одну велику функцію-редьюсер, яка записується в поле
                              authSlice.reducer

                    ────────────────────────────────────────────────────────────────────────────────────────────────────


   3. На цьому етапі authSlice.reducer ще жодного разу не викликався, тобто глобального стейту в додатку ще не існує.

      Він буде ініціалізований пізніше — при виклику configureStore та автоматичній передачі системного екшену
      ініціалізації { type: '@@redux/INIT' } у глобальний редьюсер.


   4. Структура згенерованого API-об'єкта:

                                   ┌────────────────── authSlice ───────────────────┐
                                   │                                                │
                                   │   {                                            │
                                   │     name: 'auth',                              │
                                   │     actions: { setError: f, clearToken: f },   │
                                   │     reducer: f(state, action)                  │
                                   │   }                                            │
                                   │                                                │
                                   └────────────────────────────────────────────────┘

      З якої ми потім десруктуризуємо й експорутємо наші actinCreators (для того щоб потім використати в компонентах):

                                   ┌────────────────────────────────────────────────┐
                                   │                                                │
                                   │ export const { setError } = authSlice.actions  │
                                   │                                                │
                                   └────────────────────────────────────────────────┘




═══════════════════════════════
 Асинхронні Action Creators

 📍 Thunk Action Creators
═══════════════════════════════


   СТВОРЕННЯ:
═══════════════

 Функція createAsyncThunk повертає Thunk Action Creator, який ми зберігаємо, наприклад, у змінну verifyCode.


         ┌────────────────────────────────── Створення Thunk Action Creator ──────────────────────────────────┐
         │                                                                                                    │
         │    export const verifyCode = createAsyncThunk(                                                     │
         │       'auth/verifyCode',                                                                           │
         │        async ({ code }, { getState, dispatch, rejectWithValue }) => {...}    ───> asyncCallback    │
         │    )                                                                                               │
         │                                                                                                    │
         └────────────────────────────────────────────────────────────────────────────────────────────────────┘



 verifyCode ─ це Thunk Action Creator (творець thunk-екшенів), тобто функція яка повертає thunk.


 createAsyncThunk під капотом створює й повертає Thunk Action Creator ─ це функція, яка представляє з себе конструкцію з
 трьох вкладених одна в одну функцій.

  1 рівень: Thunk Action Creator ─ вона викликається всередині dispatch й повртає thunk

  2. thunk в свою чергу всередині себе викликає asyncCallback, який ми передали другим аргументом в createAsyncThunk

  3. asyncCallback ─


════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
                                     РІВЕНЬ 1: verifyCode — сам Thunk Action Creator
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════


  ІНІЦІАЛІЗАЦІЯ:
══════════════════

 ┌─────────────────────────────────────────────────────────────────────────────────┐
 │  export const verifyCode = createAsyncThunk('auth/verifyCode', asyncCallback);  │
 └─────────────────────────────────────────────────────────────────────────────────┘
                                     ⬇
        Змінна verifyCode стає Thunk Action Creator, творцем thunk-екшенів



 ДЕ ВИКЛИКАЄТЬСЯ:
══════════════════

 ┌──────────────────────────────────────────┐
 │  dispatch(verifyCode({ code: '1234' }))  │
 └──────────────────────────────────────────┘

 1. Обчислення аргументу (Виклик Thunk Action Creator):

      Перед тим як виконати саму функцію dispatch, двигуну потрібно обчислити її аргумент, бо функції завжди очікують
      на вході конкретні значення (примітиви, об'єкти чи посилання на інші функції).

      Ми ж в дужках передаємо вираз, тому потік виконання (Main Thread) починає роботу зсередини назовні: спочатку
      викликається функція verifyCode({ code: '1234' }).


 2. Повернення результату в момент виклику:

      verifyCode - це синхронна функція, яка миттєво відпрацьовує і повертає свій результат — асинхронну функцію другого
      рівня (thunk).

      На цьому етапі робота самого Action Creator повністю завершена.


 3. Підстановка результату та запуск dispatch (Робота Обгортки):

      Повернений thunk підставляється замість виразу в дужках. Тепер рядок під капотом перетворюється на dispatch(thunk)
      І лише тепер викликається сам dispatch, передаючи цей thunk у мідлвару.

      Хук useDispatch() повертає нам НЕ оригінальний  диспатч Redux, а прокачану мідлварами функцію-обгортку.

    Тому, коли ми тиснемо на кнопку і викликається `dispatch()`, першим ділом запускається код
    мідлвари `redux-thunk`, яка стоїть на "зовнішніх воротах":



 Головна задача:
══════════════════

 Взяти вхідні аргументи (payload), з якими його викликали, згенерувати й повернути функцію другого рівня (thunk)


                                 ──────────────────────────────────────  Closure  ──────────────────────────────────────

                                   Оскільки thunk всередині себе звертається до payload (він передає його як аргумент
                                   при виклику asyncCallback) ─ виникає лексичне замикання (closure).

                                   Це замикання гарантує, що Garbage Collector (збирач сміття V8) не видалить ці дані з
                                   оперативної пам'яті (RAM) вкладки браузера.


                                   Двигун JavaScript побачить що thunk (Рівень 2) використовує змінну payload із
                                   зовнішньої функції й виконає два кроки:

                                       ◦ Створення Scope: Він створить в купі (Heap) спеціальний об'єкт області
                                         видимості замикання — Closure Scope й покладе туди посилання на змінну payload.

                                       ◦ Захоплення посилання: Він запише посилання на цей цей об'єкт (Closure Scope)
                                         у приховане системне поле функції (thunk).


                                   Завдяки цьому thunk буде мати прямий доступ до цих даних навіть після того, як сам
                                   Action Creator (Рівень 1) повністю завершить свою роботу й зникне з Call Stack.

                                   Й коли мідлвара згодом запустить thunk, наш асинхронний колбек гарантовано отримає
                                   саме ті дані, які були передані в момент виклику.

                                 ───────────────────────────────────────────────────────────────────────────────────────

Що вона повертає? Вона повертає функцію другого рівня (Танк).





🧬 Рівень 2: Повернена функція — це Thunk (Танк-менеджер)
Це другий рівень вкладеності у твоєму коді:

JavaScript
return async function (dispatch, getState) { ... }
Цей шматок коду в комп'ютерній інженерії називається Thunk (Танк).

Thunk — це функція, яка обгортає якусь дію, щоб відкласти її виконання на потім.

Хто її викликає? Не ти. Ти її лише диспатчиш: dispatch(verifyCode(payload)).

Мідлвара redux-thunk бачить: "О, у dispatch прилетів не об'єкт, а функція! Значить, я не пускаю її в редьюсери, а викликаю її сама і передаю в неї аргументи (dispatch, getState)".

Яка її задача? Це менеджер життєвого циклу запиту. Вона керує процесом: сама створює унікальний requestId, сама диспатчить pending, запускає твій колбек, ловить помилки через try/catch і диспатчить fulfilled або rejected.

🚀 Рівень 3: Твій asyncCallback — це Payload Creator
Це третій, найглибший рівень вкладеності, який ти сама пишеш у конфігурації:

JavaScript
async (payload, thunkAPI) => { ... }
В офіційній документації Redux Toolkit ця функція називається Payload Creator (творець корисного навантаження).

Яка її задача? Вона повністю ізольована від Redux-менеджменту. Її задача — просто піти в мережу (зробити fetch/axios), отримати «чисті» дані від сервера й повернути їх через return (або повернути помилку через rejectWithValue). Вона не знає, коли диспатчити pending чи fulfilled — за неї це робить менеджер з Рівня 2.

🏁 Шпаргалка для конспекту: "Три рівні влади в createAsyncThunk"
Щоб зафіксувати це раз і назавжди, підпиши схему з трьох рівнів так:

verifyCode (Async Action Creator) — Фабрика. Ти викликаєш її в компоненті. Задача — взяти твій payload і синхронно повернути Танк-менеджера.

async function(dispatch, getState) (Thunk / Менеджер) — Закулісний виконавець. Працює всередині мідлвари. Задача — диспатчити системні екшени (pending, fulfilled, rejected) та контролювати хід виконання.

asyncCallback (Payload Creator) — Чорнороб. Твій особистий асинхронний код. Задача — просто зробити запит до API та віддати чистий результат або помилку.





         ┌──────────────────────────────────────────────────────────────────────────────────────────────────────┐
         │                                                                                                      │
         │                                                                                                      │
         │    function verifyCode(payload) {                                                                    │
         │                                                                                                      │
         │        return async function (dispatch, getState) {                                                  │
         │                                                                                                      │
         │            dispatch({ type: 'auth/verifyCode/pending' });    —> одразу диспатчимо pending action     │
         │                                                                                                      │
         │            const rejectWithValue = (value) => {                                                      │
         │              return {                                                                                │
         │                payload: value,                                                                       │
         │                meta: { rejectedWithValue: true }                                                     │
         │              };                                                                                      │
         │            };                                                                                        │
         │                                                                                                      │
         │            const thunkAPI = { dispatch, getState, rejectWithValue };                                 │
         │                                                                                                      │
         │            try {                                                                                     │
         │                const result = await asyncCallback(payload, thunkAPI);                                │
         │                                                                                                      │
         │                if (result && result.meta?.__isRejectWithValue) {                                     │
         │                                                                                                      │
         │                    dispatch({                                                                        │
         │                        type: 'auth/verifyCode/rejected',                                             │
         │                        payload: result.payload                                                       │
         │                    });                                                                               │
         │                                                                                                      │
         │                } else {                                                                              │
         │                                                                                                      │
         │                    dispatch({                                                                        │
         │                        type: 'auth/verifyCode/fulfilled',                                            │
         │                        payload: result                                                               │
         │                    });                                                                               │
         │                }                                                                                     │
         │                                                                                                      │
         │            } catch (error) {                                                                         │
         │                                                                                                      │
         │                dispatch({                                                                            │
         │                    type: 'auth/verifyCode/rejected',                                                 │
         │                    payload: error.message                                                            │
         │                });                                                                                   │
         │                                                                                                      │
         │            }                                                                                         │
         │        }                                                                                             │
         │    }                                                                                                 │
         │                                                                                                      │
         │                                                                                                      │
         └──────────────────────────────────────────────────────────────────────────────────────────────────────┘

function verifyCode(payload) {

    // Повертається той самий системний Thunk (Менеджер)
    return async function (dispatch, getState) {

        // 1. Одразу диспатчимо pending об'єкт
        dispatch({ type: 'auth/verifyCode/pending' });

        // 2. Менеджер прямо тут, «на льоту», створює функцію rejectWithValue
        const rejectWithValue = (value) => {
          return {
            payload: value,
            meta: { rejectedWithValue: true } // зашиваємо секретний маркер
          };
        };

        // Створюємо інструменти для твоєї функції (thunkAPI)
        const thunkAPI = { dispatch, getState, rejectWithValue };

        try {
            // 2. ТУТ МИТТЄВО ВИКЛИКАЄТЬСЯ ТВОЯ ФУНКЦІЯ (Рівень 3)
            // Менеджер чекає на її проміс через await
            const result = await asyncCallback(payload, thunkAPI);

            // 3. Перевіряємо, чи ти повернула rejectWithValue
            if (result && result.meta?.__isRejectWithValue) {
                // Якщо так — формуємо rejected екшен з твоїм errorCode
                dispatch({
                    type: 'auth/verifyCode/rejected',
                    payload: result.payload
                });
            } else {
                // Якщо все ок — формуємо fulfilled екшен з даними
                dispatch({
                    type: 'auth/verifyCode/fulfilled',
                    payload: result
                });
            }

        } catch (error) {
            // 4. Якщо в твоєму коді сталася непередбачувана помилка (наприклад, TypeError)
            // Менеджер ловить її і ВСЕ ОДНО диспатчить rejected
            dispatch({
                type: 'auth/verifyCode/rejected',
                payload: error.message
            });
        }
    };
}






2. Що таке verifyCode насправді? (Аналіз трирівневої вкладеності)
createAsyncThunk при виклику повертає actionCreator, а не thunk


При виклику dispatch(verifyCode({ email, code })) запускається чітка трирівнева структура:

📦 Рівень 1: verifyCode (Action Creator)
Це звичайна синхронна функція-фабрика, згенерована Redux Toolkit. Коли ти пишеш verifyCode({ email, code }), вона миттєво виконується на місці й повертає Функцію-Танк.

На цьому етапі ніякий мережевий запит ще не запускається. Ми просто створили інструкцію для Redux.

🔄 Рівень 2: Thunk-обгортка (Повернута функція-менеджер)
Ця Функція-Танк залітає в store.dispatch. Thunk Middleware перехоплює її та запускає в роботу. Ця обгортка є головним менеджером процесу, який синхронізує стан запиту з Redux-стором:

Вона миттєво робить dispatch({ type: 'auth/verifyCode/pending' }), щоб увімкнути лоадери на UI.

Тільки після цього вона викликає код твоєї асинхронної функції (Рівень 3).

Менеджер завжди очікує повного завершення цього коду.

⚡ Рівень 3: Твій асинхронний колбек (Код з fetch та try/catch)
Тут виконується написана тобою логіка: запит до API, перевірка res.ok, запис у localStorage та перехоплення помилок мережі.

З точки зору JavaScript, цей рівень повертає результат роботи твоєї функції:

Або чисті дані через return data.

Або спеціальний об'єкт помилки через return rejectWithValue(...).

🛡️ Фінальний акорд менеджера: Чому проміс ЗАВЖДИ успішний?
Коли твій асинхронний код на Рівні 3 повністю завершується, менеджер (Рівень 2) перехоплює результат і аналізує його:

Якщо код повернув дані (return data): Менеджер робить dispatch({ type: 'auth/verifyCode/fulfilled', payload: data }). Потім сам Танк завершує свою роботу і повертає в код додатка успішний (resolved) проміс із цим екшеном усередині.

Якщо код повернув помилку (return rejectWithValue): Менеджер робить dispatch({ type: 'auth/verifyCode/rejected', payload: ... }). Потім сам Танк завершує свою роботу і ВСЕ ОДНО повертає успішний (resolved) проміс, всередині якого просто лежить екшен зі статусом помилки.

createAsyncThunk примусово гасить будь-які відхилені проміси на виході з Танка, щоб вберегти додаток від падіння (Unhandled Promise Rejection).

Штучно перетворити цей гарантовано успішний проміс на справжній JavaScript-негатив (викинути throw error в локальний catch) може тільки метод .unwrap(), який ми викликаємо вручну в компоненті.

🏁 Залізобетонний підсумок
verifyCode — це Action Creator.

Виклик verifyCode(...) створює Thunk (функцію-менеджера).

Цей Thunk запускається через dispatch, виконує асинхронний код і завжди повертає успішний проміс, маркуючи успіх чи невдачу суто через типи екшенів (fulfilled / rejected).



                                           ⬇

                     ┌────────────────────────── Викликаємо actionCreator ────────────────────────┐
                     │                                                                            │
                     │  const action = await dispatch(verifyCode(code))                           │
                     │                                                                            │
                     └────────────────────────────────────────────────────────────────────────────┘

                     ════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
                                     Що саме можна передавати в store.dispatch?
════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════






 */