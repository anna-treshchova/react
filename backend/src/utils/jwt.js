import jwt from 'jsonwebtoken';

const JWT_SECRET = process.env.JWT_SECRET;
const JWT_EXPIRES_IN = process.env.JWT_EXPIRES_IN;

export function signAccessToken(payload = {}) { // payload — об’єкт даних, які кладемо у токен (мінімум user.id)
    if (!JWT_SECRET) {
        throw new Error('JWT_SECRET is not defined in .env');
    }

    return jwt.sign(payload, JWT_SECRET, {
        expiresIn: JWT_EXPIRES_IN,
        algorithm: 'HS256',
        }
    )
}


/*——————————————————————————————————————————————————————————————————————————————————————————————————————————————————————

 jwt.sign — це метод бібліотеки jsonwebtoken, який створює JWT-токен (цей процес також називають підписанням токена).  
 
 Результатом є спеціальний рядок, що складається з 3 частин, розділених крапками:

                                       ┌──────────────────────────┐
                                          (1)     (2)     (3)
                                         header.payload.signature
                                       └──────────────────────────┘



                              МЕТОД jwt.sign ПРИЙМАЄ ТРИ ОСНОВНІ АРГУМЕНТИ:
                             ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾ 

 1. payload — об'єкт із даними, які ми хочемо вкласти в токен (друга частина JWT)
‾‾‾‾‾‾‾‾‾‾‾
       ВАЖЛИВО: Друга частина JWT не зашифрована, а лише закодована в Base64url.
                НЕ КЛАСТИ ЧУТЛИВІ ДАНІ (паролі, номери карток).
                
        

              —————————————————————————————  ДАНІ ЯКІ ЗАЗВИЧАЙ ПЕРЕДАЮТЬ В PAYLOAD:  ————————————————————————————————
                                  

                   1.1  id — унікальний ID користувача
                   ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

                         Він необхідний для ідентифікації власника токена. 
            
                         ID дозволяє серверу робити пошук по базі даних ідентифікуючи користувача, 
                         якому належить токен, та отримувати його специфічні дані:  
                    
                             ◦ списки бажань, 
                             ◦ історію бронювань
                             ◦ налаштування профілю тощо

                    
                   1.2  role — 'user', 'admin', 'manager', 'guest'
                   ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
                   
                         Оскільки ми не можемо довіряти фронтенду на 100%, кожен раз при запиті на отримання 
                         чутливих сторінок (wishlist), або чутливих даних чи прав на зміну цих даних (для admin),
                         СЕРВЕРУ ДОВЕЛОСЯ Б:
             
                             -> проходитись по масиву користувачів
                    
                             -> знаходити необхідного юзера за допомогою id, витягнутого з токена
        
                             -> перевіряти, чи дозволяє його роль отримати ці дані


                        Передаючи role в payload токена, ми скіпаємо цей процес, оскільки даним з токена сервер може 
                        довіряти на 100%.
             
                        Бо якщо в токен внести зміни з боку фронтенду, він не пройде перевірку порівняння секретного 
                        підпису jwt.verify()
             
                        Таким чином ми уникаємо постійних безперервних пошуків у базі даних, роблячи перевірку прав
                        миттєвою



            
              ——————————————————  ДАНІ ЯКІ ДОДАЮТЬСЯ В PAYLOAD АВТОМАТИЧНО ПРИ СТВОРЕННІ ТОКЕНА:  ———————————————————
                                                    
                 Коли токен створюється, бібліотека jsonwebtoken автоматично додає у payload два службові 
                 (стандартні) поля:


                   1.3  iat (issued at — видано о) — час створення токену (unix timestamp)
                   ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
                                      

                   1.4  exp (expires at — закінчується о) — час коли цей токен стане недійсним (unix timestamp) 
                   ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

                         Він обчислюється автоматично: iat (час створення) + expiresIn (час життя токена)
             
                         Об'єкт з властивістю expiresIn ми передаємо як третій аргумент у метод jwt.sign 
                         (options — налаштування)

                 
                                                     
                                                     ———————————————————————————————————————————————————————————————————
                                                                           ПРИКЛАД PAYLOAD:

                                                         {
                                                            id: 42,             —> унікальний ID користувача
                                                            role: 'user',       —> або 'admin', 'manager' тощо
                                                            iat: 1730572086,    —> автоматично додається: issued at
                                                            exp: 1730575686     —> автоматично додається: expires at
                                                          }
                                                     ———————————————————————————————————————————————————————————————————



 2. JWT_SECRET — секретний ключ 
‾‾‾‾‾‾‾‾‾‾‾‾‾‾  
        
        ◦ Потрібен для створення signature (підпису) — третьої частини токена

        ◦ Зберігається в .env як JWT_SECRET

        

        
 3. { expiresIn, algorithm } — налаштування (options)
‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
                                                                   
        3.1  expiresIn                 
        ‾‾‾‾‾‾‾‾‾‾‾‾‾‾                           
             ◦ Визначає скільки часу токен буде дійсним (наприклад, '1h', '7d')
             
             ◦ Зберігається в .env як JWT_EXPIRES_IN
             
             ◦ DEFAULT відсутній, тобто якщо не предати це поле токен буде вічний й ніколи не протермінується



        3.2 algorithm                        
        ‾‾‾‾‾‾‾‾‾‾‾‾‾
             ◦ Визначає, через який криптографічний алгоритм буде обчислюватися підпис токена (signature),
               тобто яким способом рядок DataToSign буде видозмінений й пеерроблений для того щоб дані в ньому неможливо прочитати



  Слово КРИПТОГРАФІЧНИЙ походить від грецьких слів kryptos (прихований)
                          та graphein (писати). Дослівно — це тайнопис.
             ◦ DEFAULT: 'HS256'.

             ◦ ЯК ПРАЦЮЄ АЛГОРИТМ HS256 (HMAC + SHA-256):

                   • HMAC — це механізм, який бере Secret Key та DataToSign 
                                 
                   • Змішує їх спеціальним криптографічним способом         

                   • Отриманий результат обробляється (хешується у два етапи) хеш-функцією SHA-256

————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————              
  DataToSign — рядок, який складається з вже закодованих через base64url та склеєних через крапку Header та Payload
———————————————————————————————————————————————————————————————————————————————————————————————————————————————————————— 



———————————————————————————————————————————————————  СТВОРЕННЯ ТОКЕНУ  —————————————————————————————————————————————————




                                               1. РОБОТА З JS ОБ'ЄКТАМИ                                                
                   ┌──────────────────────────────────────────────────────────────────────────────────────┐
                   │                                               │                                      │ 
                   
┌──────────────── 1.1 ────────────────┐     ┌──────────────────── 1.2 ───────────────────┐     ┌───────── 1.3 ────────┐
│                                     │     │                                            │     │                      │
│   jwt.sign(payload, JWT_SECRET, {   │     │   payload = {          payload = {         │     │    header = {        │
│     expiresIn: JWT_EXPIRES_IN,        ──>       userId: 1,    ─>     userId: 1,          ──>        alg: 'HS256',   │ 
│     algorithm: 'HS256',             │     │     role: 'user'         role: 'user',     │     │      typ: 'JWT'      │
│   })                                │     │   }                      iat: 1715000000,  │     │    }                 │
│                                     │     │                          exp: 1715003600   │     │                      │  
│                                     │     │                        }                   │     │                      │
└─────────────────────────────────────┘     └────────────────────────────────────────────┘     └──────────────────────┘
  
    Ми предаємо в метод jwt.sign               Бібліотека jsonwebtoken автоматично додає         Також створює хедер 
    три аргументи:                             до об'єкта payload два службових поля

     ◦ payload - object
     ◦ JWT_SECRET - string 
     ◦ options - object 




        
                                2. DATA PREPARATION PIPELINE ПЕРЕД СТВОРЕННЯМ SIGNATURE                                              
                   ┌──────────────────────────────────────────────────────────────────────────────┐
                   │                         │                            │                       │ 
                    
┌─────────────┐   2.1   ┌───────────────┐   2.2   ┌──────────────────┐   2.3   ┌─────────────┐   2.4   ┌──────────────────┐                                  
│  JS Object  │  ────>  │  JSON string  │  ────>  │  UTF-8 encoding  │  ────>  │  Base64URL  │  ────>  │  Crypto signing  │                                                            
└─────────────┘         └───────────────┘         └──────────────────┘         └─────────────┘         └──────────────────┘


───────
Pipeline — це послідовний ланцюжок етапів, де дані входять в одну сторону, проходять через серію трансформацій, 
або краще сказати серіалізацій (тобто видозмінюються) і виходять з іншої сторони вже в потрібному нам форматі
───────

   JWT не працює напряму з JavaScript об'єктами
   Оскільки криптографічні алгоритми JWT працюють лише з байтами, дані проходять декілька етапів серіалізації, перед тим як почнеться 
   процес створення підпису


        
                                           
——————————————————————————————————————————————  UTF-8 (Кодування тексту)  ——————————————————————————————————————————————

                                                                                                         
  Unicode 
‾‾‾‾‾‾‾‾‾‾‾
      Unicode (Юнікод) — це назва глобального стандарту, яка походить від поєднання слів: universal та code
      (універсальний код)

      СУТЬ: Це єдиний стандарт (система), де кожному символу світу (літері, цифрі, емодзі) присвоєно унікальне число — 
      Code Point (кодова точка, тобто НОМЕР)

                                
      НАПРИКЛАД: символ «A» — це номер 65, а емодзі «🚀» — це номер 128640

      ВАЖЛИВО: Unicode — це не суворий технічний стандарт, задокументований міжнародною організацією


      
   UTF-8
‾‾‾‾‾‾‾‾‾‾‾                           
      UTF-8 (Unicode Transformation Format — Формат перетворення Юнікоду) — це стандарт кодування, який працює на базі 
      системи Unicode

           —>  ТОБТО Unicode призначає кожному символу унікальний номер (Code Point)
                                 
           —>  А UTF-8 бере цей номер й перетворює його у послідовність байтів, яка може варіюватися від 1 до 4 в 
               залежності від того, наскільки великий Code Point

          
┌———————————————————————————————————————————————————————————————————————————————————————————┐
│  Unicode (це Таблиця): Яка відповідає на питання "Яке число закріплене за цим символом?"  │
│                                                                                           │
│  UTF-8 (це Алгоритм): Який відповідає на питання "Як це число записати в байти?"          │
└———————————————————————————————————————————————————————————————————————————————————————————┘




                                                             КРОКИ ПЕРЕТВОРЕННЯ ТЕКСТУ В БАЙТИ                                             
                    ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
                    │                                                      │                                                            │ 
                     
┌────────────────  2.1 ─────────────────┐        ┌──────────────────────  2.2 ────────────────────────┐        ┌─────────────────────  2.3 ─────────────────────────┐  
│                                       │        │                                                    │        │                                                    │ 
│                 СИМВОЛ                │        │                 ЧИСЛО (Code Point)                 │        │               БАЙТИ (Encoding Result)              │ 
│     Те, що ми бачимо на екрані        │  ───>  │    Абстрактне число, яке відповідає конкретному    │  ───>  │   Безпосередній запис числа у пам'ять комп'ютера   │ 
│                                       │        │            символу в стандарті Unicode             │        │                      (0 і 1)                       │                               │ 
│                                       │        │                                                    │        │                                                    │ 
│                                       │        │                                                    │        │                                                    │ 
   ПРИКЛАД: Літера "G" або емодзі "🚀"              ПРИКЛАД: "G" завжди має номер 71, а "🚀" — 128640
└───────────────────────────────────────┘        └────────────────────────────────────────────────────┘        └────────────────────────────────────────────────────┘        
    
      
 КРОК 2.3
‾‾‾‾‾‾‾‾‾‾‾                               
      Саме тут працює алгоритм UTF-8
                                     
      На відміну від UTF-32, він не витрачає зайвих байтів, а використовує лише стільки, скільки потрібно, щоб 
      записати Code Point

 ——————————————————————————————————————————————————————
                 1 байт = 8 бітів

  Максимальне число, яке можна покласти в 1 байт: 255
                                                            
  Мінімальне число: 0
—————————————————————————————————————————————————————— 



                                                              ПОРІВНЯННЯ СТАНДАРТІВ                                          
                                  ┌────────────────────────────────────────────────────────────────────────┐
                                  │                                                                        │ 
                     
┌────────────────────────────  UTF-32 ─────────────────────────────┐    ┌──────────────────────────────  UTF-8  ─────────────────────────────────┐      
│                                                                  │    │                                                                        │
│   Марнотратний спосіб запису, в якому будь-яке число,            │    │   Економічний спосіб запису, який використовує лише стільки байтів,    │
│   навіть маленьке, завжди займає 4 байти                         │    │   скільки реально потрібно, щоб вмістити необхідне число               │
│                                                                  │    │                                                                        │ 
│     Символ "G" (номер 71) буде записаний як: 00 00 00 47         │    │                                                                        │ 
│                                                                  │    │    Він діє за принципом економічності:                                 │ 
│    РЕЗУЛЬТАТ: Ми витратили три зайві байти вхолосту.             │    │                                                                        │ 
│    Текст важить у 4 рази більше.                                 │    │       ◦ Якщо число мале (як 71), він записує його в 1 байт             │ 
│                                                                  │    │                                                                        │ 
│                                                                  │    │       ◦ Якщо число велике (як 128640), він розбиває його на 4 байти    │                                                                  
│                                                                  │    │                                                                        │ 
│                                                                  │    │                                                                        │ 
└──────────────────────────────────────────────────────────────────┘    └────────────────────────────────────────────────────────────────────────┘  
                                                                     ⬇ 
                                                                     ⬇
                                                                                                                        
                                                                 
                  ┌───────────────────────────────────────────────  ПІДСУМОК  ───────────────────────────────────────────────┐
                           
                      ◦ Unicode та UTF — це стандарти, що стосуються ЛИШЕ ТЕКСТУ
                                                                     ‾‾‾‾‾‾‾‾‾‾‾
                      ◦ Вони забезпечують повний цикл перетворення символів у цифровий вигляд:
                                                    
                           • Кодування (Encoding) — процес перетворення тексту (символів) у послідовність байтів   
                             ТЕКСТ —> БАЙТИ


                           • Декодування (Decoding) — процес перетворення байтів назад у зрозумілий людині текст (символи) 
                             БАЙТИ —> ТЕКСТ
                             
                  └──────────────────────────────────────────────────────────────────────────────────────────────────────────┘

















————————————————————————————————————  Base64URL (Кодування бінарних даних у текст)  ————————————————————————————————————



Base64 — це стандарт кодування, який перетворює будь-які бінарні дані (байти) у рядок, що складається лише із текстових символів.

СУТЬ: На відміну від UTF-8, який робить із тексту байти, Base64 робить навпаки — він бере байти (будь-які: картинки, зашифровані дані, файли)
і перетворює їх на текст, який зручно передавати.

Чому "64"?
Алгоритм використовує обмежений набір із 64 символів для представлення будь-яких даних:

Велика латиниця (A–Z) — 26 символів.

Мала латиниця (a–z) — 26 символів.

Цифри (0–9) — 10 символів.

Три спецсимволи: Плюс (+), Слеш (/) та Дорівнює (=)
ни рядка, так званий "padding").

Навіщо це потрібно?
Багато комп'ютерних систем (особливо старі поштові сервери або протоколи) були розраховані лише на передачу тексту. Якщо спробувати передати через них "сирі" байти (наприклад, файл картинки), система може сприйняти випадковий байт як команду "кінець файлу" або "перенос рядка", і дані пошкодяться.

Base64 вирішує цю проблему: він запаковує "небезпечні" байти у набір "безпечних" латинських літер та цифр.

Приклад логіки:
Маємо файл (байти).

Base64 розбиває ці байти на групи.

Кожну групу він замінює на символ із таблиці (A-Z, a-z, 0-9, +, /).

Результат: Довгий незрозумілий рядок на кшталт SGVsbG8h.

ВАЖЛИВО ДЛЯ ПОРІВНЯННЯ:
Головний мінус класичного Base64 — символи +, / та =.

В адресному рядку браузера (URL) символ / означає шлях до папки.

Символ + в URL часто перетворюється на пробіл.

Символ = має спеціальне значення для параметрів.

Саме через це з'явився Base64URL, про який ми поговоримо далі.



                        

2. Base64URL (Кодування бінарних даних у текст)
Base64URL — це спосіб представлення будь-яких байтів (картинок, ключів шифрування, уже готового UTF-8 тексту) у вигляді безпечного тексту.

Суть: Це транспортний формат. Він бере довільний набір бінарних даних і перетворює їх на обмежений набір з 64 символів (латиниця, цифри, - та _).

Сфера застосування: Використовується там, де система "не любить" складні символи (наприклад, в URL-адресах, HTTP-заголовках або JWT-токенах).

Головна мета: Перетворити «небезпечні» або нечитабельні байти на «безпечний» рядок, який не зламає посилання в браузері.
   
             
———————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————————


 
                                2. DATA PREPARATION PIPELINE ПЕРЕД КРИПТОГРАФІЧНИМ ПІДПИСАННЯМ                                     
                   ┌──────────────────────────────────────────────────────────────────────────────┐
                   │                         │                            │                       │ 
                    
┌─────────────┐   2.1   ┌───────────────┐   2.2   ┌──────────────────┐   2.3   ┌─────────────┐   2.4   ┌──────────────────┐                                  
│  JS Object  │  ────>  │  JSON string  │  ────>  │  UTF-8 encoding  │  ────>  │  Base64URL  │  ────>  │  Crypto signing  │                                                            
└─────────────┘         └───────────────┘         └──────────────────┘         └─────────────┘         └──────────────────┘

——————————————————————————————————————————————  UTF-8 (Кодування тексту)  ——————————————————————————————————————————————

───────
Pipeline — це послідовний ланцюжок етапів, де дані входять в одну сторону, проходять через серію трансформацій, 
або краще сказати серіалізацій (тобто видозмінюються) і виходять з іншої сторони вже в потрібному нам форматі
───────


 — Чому Data Preparation Pipeline (Підготовка даних перед криптографічним підписанням) має так багато етапів?
   ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

 — Проходити через стільки етапів нас змушує фундаментальна вимога до створення Signature — ДЕТЕРМІНІЗМ 
   (однакові вхідні дані мають завжди давати однаковий результат)


Для того щоб прослідкувати чому кожен етап цього пайплайн критично важливий й логічно залежний від попереднього етапу ми прослідкуюємо логіку від зворотнього

  1. КІНЦЕВА МЕТА: HTTP Header (Заголовок)

        Токен має передаватися в полі HTTP заголовка Authorization. 
        Оскільки HTTP — це текстово-орієнтований протокол, це диктує особливі вимоги до формату даних.


        Проблема інтерпретації (Parsing)
На відміну від бінарних протоколів (IP, TCP), де програма працює за суворою сіткою бітів (наприклад, "біти з 1 по 16 — це порт"), 
програма-отримач HTTP (браузер або сервер) сприймає байти хедера як текстовий рядок.

Логіка бінарних протоколів: Працюють із байтами як із числовими значеннями. Їм байдуже, який вигляд має байт, головне — його позиція.

Логіка HTTP: Шукає в потоці байтів спеціальні маркери-розділювачі (пробіл, двокрапка, перенос рядка).

Чому "сирі" байти небезпечні?
Якщо записати байти криптографічного підпису в хедер напряму, виникає ризик:

Конфлікт із керуючими символами: Випадкова комбінація байтів підпису може збігатися з маркером CRLF (перенос рядка \r\n).

Збій парсингу: Програма-отримач прийме цей байт за команду "кінець заголовка" і обірве зчитування токена посередині. Запит буде понівечено (Помилка 400 Bad Request).

Рішення: Base64URL
Він працює як стерилізатор. Будь-який набір бінарних даних перетворюється на обмежений набір безпечних ASCII-символів (літери та цифри). Це гарантує, що текстовий парсер HTTP прочитає весь токен як один суцільний рядок, не зустрівши всередині жодної "міни" у вигляді керуючого байта.



Проблема:  HTTP-протокол навідміну від бінарних протоколів (IP, TCP) є текстово-орієнтованим.


Це означає, що програма-отримач (браузер або веб-сервер), яка обробляє хедери, 
зчитує байти з хедеру й намагається відразу перетворити їх на текстовий рядок.

Програма-отримачі ж бінарних протоколів зчитують й працюють саме з байтами






Проблема інтерпретації: Якщо ми спробуємо передати байти криптографічного підпису "як є", серед них обов'язково трапляться комбінації, які в таблиці символів (ASCII/UTF-8) означають службові команди. Наприклад:

0x0D 0x0A — програма сприйме це як кінець заголовка, і все, що йде далі, просто відріжеться.

0x00 — програма може подумати, що це кінець всього повідомлення.

Результат помилки: Програма-парсер "спіткнеться" об такий символ, некоректно розіб'є заголовок на частини, і токен не буде прочитаний або запит взагалі зламається.

Рішення (Base64URL):
Ми перепаковуємо будь-які байти у набір символів, які гарантовано не мають спеціального (керуючого) значення в протоколі HTTP. Тепер програма спокійно зчитує заголовок як звичайний текст, не зустрічаючи "мін" у вигляді службових байтів.



        Це текстове поле. Там не може бути "сирих" байтів, особливо від криптографічного підпису (який виглядає як "сміття").
        Рішення: Все має бути запаковано в Base64URL.
        
        
        
        2. Проблема Base64URL: Він приймає лише байтиЦей алгоритм не знає, що таке "об'єкт JS". Він вміє працювати тільки з байтами. Отже, нам треба перетворити наші дані (Header та Payload) на байти.3. Проблема "прямого" перетворення: Відсутність стандартуЯк перетворити JS-об'єкт {id: 1} в байти напряму?Різні мови програмування (JS, Python, PHP) зроблять це по-різному.Навіть у JS порядок ключів в об'єкті не завжди гарантований.Наслідок: Якщо потік байтів буде різним, то і Base64URL буде різним, а отже — підпис не зійдеться.4. Рішення: JSON як "Спільний знаменник"Щоб гарантувати, що сервер на Node.js і сервер на Python зрозуміють об'єкт однаково, ми спочатку перетворюємо його на JSON-рядок. Це суворий текстовий стандарт.5. Остання ланка: UTF-8JSON-рядок — це все ще символи. Щоб Base64URL зміг їх "з'їсти", ми перетворюємо цей рядок у байти за стандартом UTF-8. Тепер ми маємо стабільний потік байтів, який не зміниться від сервера до сервера.📝 Підсумок ланцюжка (для конспекту):JS Object $\rightarrow$ початкові дані.JSON $\rightarrow$ приводимо до єдиного текстового стандарту (щоб у всіх був однаковий рядок).UTF-8 $\rightarrow$ перетворюємо стандартний текст у стандартні байти.Base64URL $\rightarrow$ робимо байти безпечним текстом для URL/Header.Crypto $\rightarrow$ бере цей текст і підписує його, щоб гарантувати: "цей рядок ніхто не міняв".Твій головний висновок: JSON потрібен для того, щоб "зацементувати" структуру об'єкта. Без нього ми б отримували різні байти для однакових даних, і криптографічний підпис було б неможливо перевірити.

───────
Серіалізація — це процес перетворення об’єкта або структури даних у послідовний формат, придатний для передачі 
або збереження (JSON, байти, Base64)
───────

——————————————————————————————————————————————  UTF-8 (Кодування тексту)  ——————————————————————————————————————————————


Для того щоб виконувати криптографічні операції,


У JavaScript engine (движка) не має єдиного стандартизованого правила як переводити дані які зберігаються в вигляді коду
(об'єктів, масивів) у байти.

 Об'єкти з однаковими даними можуть дати різний потік байтів: 


ПРИКЛАД: 

 const user = {
  id: 5,
  name: "Anna"
};


Можливі варіанти:

Варіант A
id=5;name=Anna

Варіант B
name=Anna&id=5

тому дані перед преводом в байти завжди проходять через json, який стндартизує дані у вигляаді коду зажди однаково,
json розуміють усі мови й з json два однакових потоки байтів

         
—————————————— 4 ——————————————                      ————————————————————— РОБОТА З JS ОБ'ЄКТАМИ  ——————————————————————

                                            Криптографія НЕ працює з рядками, лише з байтами


 1. Беремо header (JSON) і кодуємо у Base64url
                                                           —————————————————————————————————————————————————————————————
                                                                      ❗️Base64url — це не шифрування ❗️

                                                             Це лише спосіб представлення тексту, а не метод безпеки

                                                             Він просто перетворює двійкові байти у звичайні символи
                                                             (щоб токен можна було передати в URL чи JSON)
                                                           —————————————————————————————————————————————————————————————

         ◦ header — це службові метадані токена (який алгоритм і який тип):   { "alg": "HS256", "typ": "JWT" }
                                                                              ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
                 —> algorithm вказує, яким способом токен буде підписано

                     - за замовчуванням бібліотека jsonwebtoken використовує HS256
                     - але можна вказати інший алгоритм у параметрі algorithm у третьому аргументі методу jwt.sign

                 —> type — просто позначає тип токена (JWT) — це не обов’язкове поле, але його додають для стандарту


 2. Беремо payload (JSON, з iat, exp тощо) і кодуємо у Base64url


 3. НА ЦЬОМУ ЕТАПІ МИ ВЖЕ МАЄМО ДВІ ВІДКРИТІ ЧАСТИНИ ТОКЕНУ
    (які будь-хто може прочитати через декодування Base64url)

        ◦ headerPart = base64url(header)
        ◦ payloadPart = base64url(payload)

             token = headerPart + '.' + payloadPart  + '.' + signaturePart  —> missing part
                                                    └─────────────────────┘

 4. СТВОРЕННЯ SIGNATURE

     4.1  Склеюємо headerPart та payloadPart щоб отримати ДАНІ ПІДПИСУ:  dataToSign = headerPart + '.' + payloadPart
                                                                         ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

     4.2  Обчислюємо підпис за допомогою HMAC-SHA256 — алгоритму хешування шляхом змішування секрету з даними,
          який забезпечує криптографічну стійкість (неможливість підробки)

              rawSignature = HMAC_SHA256(secret, dataToSign)
              ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

              —> HMAC-SHA256 хешує дані разом із секретом і створює криптографічний відбиток (хеш), який неможливо
                 відновити назад

                 HS256 повертає масив байтів (не рядок) (всього 32 байти для SHA256)

     4.3  Кодуємо rawSignature у Base64url —> отримуємо signaturePart


 5. ЗБИРАЄМО ФІНАЛЬНИЙ ТОКЕН:  token = headerPart + '.' + payloadPart + '.' + signaturePart
                               ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾


————————————————————————————————————————————————————  ↓ ↓ ↓  ———————————————————————————————————————————————————————————

                                             КОЛИ СЕРВЕР ОТРИМУЄ ТОКЕН:
                                            ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾

 1. Розбиває токен на три частини: headerPart, payloadPart, signaturePart

 2. Заново обчислює signature на основі отриманих dataToSign  —> headerPart + '.' + payloadPart

       2.1  rawSignature = HMAC_SHA256(secret, dataToSign)

       2.2  expectedSignature = Base64url(rawSignature)


 3. ПОРІВНЮЄ: expectedSignature === signaturePart

      — Якщо так —> токен автентичний і не змінювався
      — Якщо ні —> токен підроблено або прострочено (exp)


 ВАЖЛИВО:

 ◦ Зі значення signature неможливо (практично) відновити secret, тому що HMAC — одностороння криптографічна операція

 ◦ СЕРВЕР НЕ РОЗШИФРОВУЄ ТОКЕН, а перевіряє його цілісність через повторне обчислення підпису з тим самим секретом



Так чи правильно я розумію, я хочу йти далі вже писати код й мені важлив чи маю я читке а головне правильне уявлення про логіку та роботу jswtoken:



1. Хедер та пейлоад та секрет проходять Data Prepartion Pipeline. Хедер та пейлоад склеюються через крапку -> це DataToSign

2. Тепер вже вступають в роботу криптографічні алгоритми для створення signature, вони отримуують DataToSign (рядок) та секрет(рядок), кодують їх, бо криптографія працює лише з байтами. 

3. Далі йде криптографічний процес де ці дві частини склеюються, пропускаються через маску (кодуються, або видозміюються за конкретними алгоритмами та умовами)

4. Вже закодовваний криптографічно бінарний потік хешується (знову видозмінюється)

5. Потім знову береться секрет вже й байтах, до нього приміняється ще одна маска (відмінна від першої), ці дайти знову додаються до вже хешованих даних й знову хешуються

6. Потім трмані байти декодуються за допомогою Base64URL й додаються через крапку до DataToSign

7. Токен готов
Data Preparation Pipeline
Кожна частина (Header та Payload) проходить індивідуальну підготовку:

jwt токен скаладється з трьох частин розділених крапкою.

1. Data Preparation Pipeline: Header та Payload проходять індивідуальну  підготовку (JS -> JSON -> UTF-8 -> Base64URL)

2. Header та Payload склеюються через крапку — це DataToSign

3. Підготовка Секрету: Секрет кодується в байти й підгоняється до стандартної довжини (64 байти). 
       
       Це технічна вимога побітової операції XOR (eXclusive OR): для коректного побітового порівняння довжина секрету має бути ідентичною довжині маски (64 байти).


       Як відбувається підготовка: 
       
            - Якщо секрет коротший за 64 байти (найчастіший випадок). До нього просто додаються нулі у кінець, доки не вийде рівно 64 байти. (Ніяке хешування не проводиться)
           
            - Якщо секрет довший за 64 байти: Він пропускається через хеш-функцію SHA-256 (найпопулярніша).

                 Хеш-функції (SHA-256) все одно, скільки байтів отримувати на вході. Вона може прийняти будь-який обсяг даних й на виході завжди видає результат фіксованої довжини — рівно 32 байти.
            
                 Оскільки вона повертає 32 байти, а операції XOR очікують 64, до них знову додаються нулі в кінець, щоб отримати потрібні 64.



4. Перша маска: Секрет пропускається через першу маску (операція XOR)
   
      Потік байтів секрету (64 байти) порівнюється з шаблоном маски (64 байти) побітово:

            - Якщо біти різні — записується 1.

            - Якщо біти однакові — записується 0.

      Результат: Новий набір із 64 байтів, де кожен початковий байт змінено за цим правилом.


5. До результату приклеюються байти DataToSign


6. Перше хешування: Цей потік даних хешується (Хеш-функція SHA-256)

      Дані будь-якого об’єму (у нашому випадку 64 байти зміненого секрету + байти даних) проходить через математичні умови алгоритму. 
      На виході ми отримуємо зовсім новий набір байтів фіксованої довжини (32 байти)

            - Будь-яка зміна хоча б одного біта на вході повністю змінює результат на виході.

            - Цей процес незворотний: отримати початкові дані з результату математично неможливо.


7. Друга маска: Підготовлений на 3 етапі екрет, який вже складається з 64 байтів пропускається через другу маску (відмінну від першої)

8. До результату приклеюється хеш із попереднього з 6-го кроку.

9. Друге хешування: Все це знову хешується функціє SHA-256. Резулльтат цієї операції - це фінальні байти підпису.

10. Кодування (Base64URL)

      Отримані байти підпису перетворюються на текстовий рядок формату Base64URL для технічної сумісності бінарних даних із 
      текстовим протоколом HTTP та стандартами URL

      СУТЬ СУМІСНОСТІ: Гарантія того, що програмний парсер, який читає дані HTTP-заголовок або URL-адресу символ за символом,
      не сприйме випадковий байт підпису за службову команду або розділювач.















      Сумісність із протоколом означає, що програма-парсер, яка читає HTTP-заголовок або URL-адресу символ за символом помилково не сприйме
      дані за службові команди свого протоколу
 

      Чому виникає проблема сумісності:

              1. Конфлікт із керуючими символами (HTTP)
                 
                   В основі HTTP лежить старий стандарт ASCII. 
                   
                       - У ньому перші 32 символи (0–31) — це не букви, а службові команди
                       - Символи в діапазоні 128–255 часто не мають візуального відображення або сприймаються системами як сміття

                   Приклад: Байт 00001010 (число 10) — це команда «перенос рядка» (\n)

                   ПРОБЛЕМА: Якщо наша криптографічна функція видасть байт із числом 10 всередині підпису,
                   HTTP-парсер побачить його, вирішить, що заголовок закінчився, і відкине решту токена.
                   Для нього це не дані, а команда «СТОП».

              
              2. Конфлікт із розділювачами (URL) - Якщо токен передавається прямо в URL-адресі
                     
                    URL-адреса має свою логіку побудови, де певні символи є ДОРОЖНІМИ ЗНАКАМИ:

                           ? — початок параметрів

                           / — розділювач шляху

                           & — зв'язка між параметрами

                    ПРОБЛЕМА: Якщо в сирому підписі трапиться байт, який у таблиці Unicode/ASCII відповідає знаку ?,
                    браузер сприйме все, що йде після нього, як параметри запиту, а не як частину нашого токена
      
    
      РІШЕННЯ: Алгоритм перекодовує будь-який бінарний потік у рядок, що складається лише з безпечних символів:
               
               - латинських літер,
               - цифр, 
               - знаку дефіса (-) та підкреслення (_)

      РЕЗУЛЬТАТ: Це гарантує що жоден програмний парсер не інтерпретує дані помилково, забезпечуючи цілісну доставку токена
      від сервера до клієнта


                  

          

Фінал: Цей текст додається через крапку до $DataToSign$. Токен готовий.






Це структурний маркер, який вказує програмному парсеру, де закінчується один логічний блок даних і починається інший.

Протоколи (HTTP, URL) — це не просто суцільний текст, це сувора структура. Щоб комп'ютер розумів цю структуру, йому потрібні символи-розділювачі, які мають статус «службових».

Ось конкретні приклади розділювачів, які Base64URL «обходить»:

1. Розділювачі в URL (RFC 3986)
URL-парсер — це зазвичай стейт-машина. Коли вона бачить розділювач, вона змінює свій внутрішній стан:

: (після протоколу) — «закінчився протокол, далі хост».

/ — «закінчився один сегмент шляху, далі наступний».

? — «закінчився шлях, далі почалися параметри (query string)».

= — «це ім'я ключа, після нього — значення».

& — «це значення закінчилося, далі — наступна пара ключ=значення».

Проблема: Якщо в сирому підписі трапиться байт, що відповідає знаку =, парсер «подумає», що все, що було до нього — це назва змінної, а після нього — її значення. Ваша єдина частина підпису перетвориться для сервера на дві різні частини.

2. Розділювачі в HTTP (RFC 7230)
HTTP-парсер шукає свої маркери для розуміння структури запиту:

  (пробіл) — розділяє метод (GET), шлях (/api) та версію (HTTP/1.1).

: — розділяє назву заголовка (Authorization) та його значення (Bearer...).

\r\n (CRLF) — найголовніший розділювач. Два такі послідовні маркери означають «секція заголовків закінчилася, далі йде тіло запиту (Body)».

Проблема: Якщо криптографія видасть байт 13 (\r) та 10 (\n), парсер HTTP вирішить, що ви щойно передали команду «кінець заголовків». Він припинить читати ваш токен і почне шукати Body, хоча токен ще не закінчився.







  Причини виникнення конфліктів:

Конфлікт із керуючими символами (HTTP):

В основі HTTP лежить стандарт ASCII. У ньому перші 32 байти (0–31) — це не текст, а керуючі команди.

Байти 128–255 часто не мають стандартного відображення або сприймаються як «бінарне сміття», що може зупинити обробку рядка.

Приклад: Байт 00001010 (число 10) — це Line Feed (\n). Якщо він з'явиться всередині підпису, парсер вирішить, що це кінець заголовка, і відсіче решту токена. Для нього це команда «СТОП».

Конфлікт із розділювачами (URL):

URL використовує спеціальні символи як «дорожні знаки»:

? — вхід у параметри запиту.

/ — розділювач шляху.

& — розділювач між параметрами.

= — присвоєння значення.

Проблема: Якщо в сирому підписі трапиться байт, що відповідає символу ?, браузер або сервер почнуть інтерпретувати решту підпису як технічні змінні запиту. Токен буде спотворено.

Рішення (Base64URL):
Алгоритм перетворює будь-який бінарний хаос (0–255) у рядок, що складається виключно з 64 безпечних символів:

Латинські літери (A-Z, a-z)

Цифри (0-9)

Спеціальні для URL: Дефіс (-) та підкреслення (_) замість небезпечних + та /.

Відсутність Padding: У версії Base64URL зазвичай видаляються знаки =, які в стандартному Base64 використовуються для вирівнювання, але в URL можуть бути сприйняті як помилкові параметри.

Результат: Цілісна доставка токена без ризику його помилкової інтерпретації мережевими вузлами.


——————————————————————————————————————————————————————————————————————————————————————————————————————————————————————*/
